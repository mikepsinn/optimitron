# Archive

Historical documents, preserved unedited. Nothing here is current: superseded
specs, dated brainstorm transcripts, and prototype documentation live here so
git history stays browsable without cluttering the live doc set.

Excluded from documentation checks. Do not update these files; if something in
here becomes relevant again, promote it back to `docs/` and rewrite it there.
