{
  "name": "@optimitron/analysis-site",
  "version": "0.1.0",
  "private": true,
  "description": "Static markdown site of all Optimitron analysis — budget, policies, legislation, efficiency rankings",
  "scripts": {
    "dev": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
