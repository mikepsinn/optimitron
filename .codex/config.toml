model = "gpt-5.5"
model_reasoning_effort = "xhigh"

approval_policy = "never"
sandbox_mode = "workspace-write"

[features]
goals = true

# MCP server entries removed 2026-05-14: when the local dev server on 3001 or
# the spawned pnpm MCP subprocess is unreachable / wedged, Codex hangs on the
# handshake at startup ("thinking forever" in the VS Code extension). Re-add
# the entries below once you have a stable dev environment.
#
# [mcp_servers.optimitron-local]
# url = "http://localhost:3001/api/mcp"
#
# [mcp_servers.optimitron-tasks]
# args = ["--filter", "@optimitron/web", "exec", "tsx", "scripts/mcp-task-server.ts"]
# command = "pnpm"
#
# [mcp_servers.optimitron-tasks.env]
# MCP_USER_EMAIL = "m@thinkbynumbers.org"
